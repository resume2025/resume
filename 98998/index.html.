<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Мое  Резюме 2025</title>
    <style>
        /* Основные стили */
        body {
            margin: 0;
            overflow: hidden;
            background-color: black;
            color: white;
            font-family: Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            text-align: center;
            position: relative;
        }

        /* Границы с неоновым светом */
        .border-glow::before,
        .border-glow::after {
            content: "";
            position: absolute;
            top: 0;
            bottom: 0;
            width: 15px;
            background: linear-gradient(180deg, #0ff, transparent, #0ff);
            box-shadow: 0 0 25px #0ff;
        }
        .border-glow::before { left: 0; }
        .border-glow::after { right: 0; }

        /* Центральный блок */
        .container {
            position: relative;
            z-index: 2;
            padding: 40px;
            border-radius: 10px;
            background: rgba(0, 0, 0, 0.9);
            border: 2px solid #0ff;
            box-shadow: 0 0 25px #0ff;
            max-width: 500px;
            animation: pulse 2s infinite alternate;
        }

        /* Пульсирующий эффект */
        @keyframes pulse {
            from {
                transform: scale(1);
                box-shadow: 0 0 25px #0ff;
            }
            to {
                transform: scale(1.02);
                box-shadow: 0 0 35px #0ff;
            }
        }

        /* Фон с пролетающими звездами */
        .stars {
            position: absolute;
            width: 100%;
            height: 100%;
            z-index: 1;
            overflow: hidden;
        }
        .star {
            position: absolute;
            background: white;
            width: 2px;
            height: 2px;
            opacity: 0.8;
            animation: moveStar linear infinite;
        }

        /* Анимация звезд */
        @keyframes moveStar {
            from {
                transform: translateY(0);
                opacity: 1;
            }
            to {
                transform: translateY(100vh);
                opacity: 0;
            }
        }

        /* Кнопка */
        .button {
            background: #0ff;
            color: #000;
            font-size: 18px;
            padding: 15px 30px;
            border: none;
            border-radius: 8px;
            cursor: pointer;
            margin-top: 20px;
            font-weight: bold;
            position: relative;
            overflow: hidden;
            transition: 0.3s;
        }
        .button:hover {
            background: #00aaff;
            box-shadow: 0 0 15px #00aaff;
        }

        /* Кнопка разлетается */
        .button:active {
            animation: explode 0.5s forwards;
        }
        @keyframes explode {
            0% { transform: scale(1); opacity: 1; }
            50% { transform: scale(1.5); opacity: 0.8; }
            100% { transform: scale(3); opacity: 0; }
        }

    </style>
</head>
<body>

    <!-- Фон со звездами -->
    <div class="stars"></div>

    <!-- Центральный блок -->
    <div class="container border-glow">
        <h1>Мое Резюме 2025</h1>
        <h2>Позиция: Разработчик</h2>
        <p>Опыт работы
 • Повар (Германия) – 1.5 года
Полностью освоил немецкую кухню, работал с профессиональным оборудованием, соблюдал стандарты качества и скорости.

 • Арбитраж трафика – 2 года
Опыт привлечения целевой аудитории, настройки рекламных кампаний, анализа эффективности и оптимизации трафика.
 
• Разработка сайтов – 3 года
Создание современных, стильных и сложных сайтов с нуля, проработка дизайна, функционала и оптимизации.

 • Кибербезопасность – 6 месяцев (в процессе обучения)
Изучение методов защиты информации, сетевой безопасности и анализа уязвимостей.</p>
        <button class="button" onclick="window.location.href='https://t.me/airdropNowa'">Связаться в Telegram</button>
    </div>

    <!-- Скрипт для звезд и разлетающейся кнопки -->
    <script>
        const starsContainer = document.querySelector('.stars');
        const numStars = 150;

        for (let i = 0; i < numStars; i++) {
            let star = document.createElement('div');
            star.classList.add('star');
            star.style.left = Math.random() * 100 + "vw";
            star.style.top = Math.random() * -100 + "vh";
            star.style.animationDuration = Math.random() * 5 + 2 + "s";
            starsContainer.appendChild(star);
        }
    </script>

</body>
</html>
